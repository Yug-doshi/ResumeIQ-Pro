# AI Resume Analyzer — Backend Dependencies
# Install with: pip install -r requirements.txt

# ── Web Framework ──
fastapi==0.115.0
uvicorn==0.30.0
python-multipart==0.0.9

# ── File Processing ──
PyPDF2==3.0.1
python-docx==1.1.0

# ── NLP & ML ──
scikit-learn==1.5.0
numpy==1.26.4
pandas==2.2.0

# ── Optional: spaCy (for better NLP) ──
# Install separately: pip install spacy && python -m spacy download en_core_web_sm
# spacy==3.7.4

# ── Optional: TensorFlow (for LSTM/GRU models) ──
# Install separately: pip install tensorflow
# tensorflow==2.16.1

# ── Optional: NLTK ──
# nltk==3.8.1

# ── Data Models ──
pydantic==2.7.0

# ── HTTP Client ──
requests==2.32.0
